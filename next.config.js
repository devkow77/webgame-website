/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['github.com', 'raw.githubusercontent.com', 'media.graphassets.com', 'lh3.googleusercontent.com'],
	},
};

module.exports = nextConfig;
